server_port: ":${MAIN_PORT}"

db:
  user: "${DB_USER}"
  password: "${DB_PASSWORD}"
  host: "postgres"
  port: "5432"
  name: "${DB_NAME}"
  sslmode: "${DB_SSLMODE}"
  timeout: ${DB_TIMEOUT}
  pool:
    min_conns: "${DB_POOL_MIN_CONNS}"
    max_conns: "${DB_POOL_MAX_CONNS}"
    max_conn_lifetime: "${DB_POOL_MAX_CONN_LIFETIME}"
    max_conn_idle_time: "${DB_POOL_MAX_CONN_IDLE_TIME}"
    health_check_period: "${DB_POOL_HEALTH_CHECK_PERIOD}"

slow_threshold: ${SLOW_THRESHOLD}